import React from 'react';
import './Header.css';

export const Header = () => (
  <header className="site-header">
    <a href="#top" className="brand" aria-label="Go to top">
      <span>WRTG 316 — Team 5</span>
    </a>
  </header>
);
